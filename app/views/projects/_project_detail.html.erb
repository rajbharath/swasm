<div class="row">
  <div class="col s12 m8">
    <div class="card">
      <div class="card-image">
        <%= image_tag(project.image) %>
      </div>
      <div class="card-content">
        <p><%= project.description %></p>
        <p><%= project.location.latitude %></p>
        <p><%= project.location.longitude %></p>
      </div>
      <div class="card-content">
        <p><%= simple_format(project.plan) %></p>
      </div>
      <div class="card-action">
        <% if current_user and policy(Project).manage? %>
          <%= link_to 'Edit', edit_project_path(project.id) %>
          <%= link_to 'Delete', project_path(project.id),  method: :delete %>
        <% end %>
      </div>
    </div>

    <% if current_user and policy(Project).manage? %>
      <% project.volunteers.each do |volunteer| %>
        <%= render partial: 'users/user', locals: { user: volunteer } %>
      <% end %>
    <% end %>
  </div>
  <div class="col s12 m4">
    <% if project.volunteers.exclude? current_user %>
      <%= link_to 'Contribute Now!', project_volunteerships_path(project), method: :post, class: 'btn waves-effect waves-light btn-large', id:'contribute-btn' %>
    <% else %>
      <span class="btn-large" id="contribute-btn">You're Awesome!</span>
    <% end %>
    <div class="card">
      <div class="card-content center-align">
        <div class="row">
          <div class="col info-box">
            <h4><%= project.specifications[:area] %></h4>
            <span>Area</span>
          </div>
          <div class="col info-box">
            <h4><%= project.specifications[:bod] %></h4>
            <span>BOD</span>
          </div>
          <div class="col info-box">
            <h4><%= project.specifications[:cod] %></h4>
            <span>COD</span>
          </div>
          <div class="col info-box">
            <h4><%= project.specifications[:ph] %></h4>
            <span>Ph</span>
          </div>
          <div class="col info-box">
            <h4><%= project.specifications[:budget] %></h4>
            <span>Budget</span>
          </div>
        </div>
      </div>
      <div class="card-action">
        <% if project.volunteers.include? current_user %>
          <%= link_to 'I leave!', project_volunteership_path(project, current_user), method: :delete %>
        <% end %>
        <%= link_to 'Go To Map', "https://www.google.com/maps/search/?api=1&query=#{ project.location.longitude },#{project.location.latitude}", target: "_blank" %>
      </div>
    </div>

    <% if current_user and policy(Event).manage? %>
      <div class="row">
        <div class="col s12">
          <div class="card">
            <div class="card-content">
              <h4 class="flow-text"><%= link_to 'Add New Event', new_project_event_path(project) %></h4>
            </div>
          </div>
        </div>
      </div>
    <% end %>

    <div class="row">
      <div class="col s12">
        <h4 class="flow-text">Event Updates</h4>
        <div class="card">
          <div class="card-content">
            <% project.events.each do |event| %>
              <span class="card-title title flow-text">
                <%= link_to event.title, project_event_path(project, event) %>
              </span>
              <p><%= event.description %></p>
              <p><strong><%= event.start_date.strftime("%B %d, %Y") %></strong></p>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
